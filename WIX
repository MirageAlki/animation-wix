<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Animation d'ouverture de porte</title>
    <style>
        /* Centrer tout sur la page */
        body {
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background-color: #ffffff;
            overflow: hidden; /* Empêche le défilement pendant l'animation */
        }

        /* Conteneur de l'animation */
        .container {
            position: relative;
            width: 300px; /* Ajuste la largeur à la taille de ton image */
            height: 300px; /* Ajuste la hauteur à la taille de ton image */
            display: flex;
        }

        /* Les deux moitiés de l'image */
        .door {
            width: 50%; /* La moitié de la largeur du conteneur */
            height: 100%;
            background-image: url('Site Internet - Design.png'); /* Image à utiliser */
            background-size: 200% 100%; /* Redimensionne pour couvrir les deux moitiés */
            transition: transform 2s ease-in-out; /* Durée et style de l'animation */
        }

        .door.left {
            background-position: left; /* Affiche la moitié gauche de l'image */
            transform-origin: left; /* Origine de la rotation à gauche */
        }

        .door.right {
            background-position: right; /* Affiche la moitié droite de l'image */
            transform-origin: right; /* Origine de la rotation à droite */
        }

        /* Classe pour ouvrir les portes */
        .door.open.left {
            transform: perspective(1000px) rotateY(-90deg); /* Rotation gauche */
        }

        .door.open.right {
            transform: perspective(1000px) rotateY(90deg); /* Rotation droite */
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="door left"></div>
        <div class="door right"></div>
    </div>

    <script>
        // Exécute l'animation après que la page est complètement chargée
        window.onload = function() {
            setTimeout(function() {
                document.querySelector('.door.left').classList.add('open');
                document.querySelector('.door.right').classList.add('open');
            }, 1000); // Délai d'une seconde avant que l'animation commence
        };
    </script>
</body>
</html>
